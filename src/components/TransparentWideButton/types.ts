export interface TransparentWideButtonProps {
    title: string,
    duration?: string,
    price?: string,
    days?: string,
    selected?: boolean,
    id?: string,
    handleChange?: VoidFunction,
    checked?: boolean,
    className?: string
}
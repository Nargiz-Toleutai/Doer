export interface ColorfulWideButtonProps {
    title: string,
    // duration?: string,
    // price?: string,
    // days?: string,
    // selected?: boolean,
    // id?: string,
    // handleChange?: VoidFunction,
    // checked?: boolean,
}